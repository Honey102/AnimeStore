# AnimeStore — Environment Variables (Frontend)
# Create a .env file in this folder and add:

# Your Render backend URL (set AFTER backend is deployed on Render)
VITE_API_URL=https://animestore-backend.onrender.com
